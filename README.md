### 📄️ Bare bones implementation of cat.
(cat - concatenate files and print on the standard output)
Should work on any Linux or BSD system.

#### To compile:
```
$ cc cat.c -o cat
```
#### Flags:
 -u    unbuffered i/o
